def export_markdown(process_flow, file_path):
"""
Markdown exporter for process flows.
"""

def export_markdown(process_flow, file_path):
    """Export a process flow to a Markdown file (stub)."""
    # TODO: Convert process_flow to markdown
    with open(file_path, 'w') as f:
        f.write("# Process Flow\n")
